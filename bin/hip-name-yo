#!/usr/bin/env ruby

require 'find'

Find.find(ENV['HOME']) do |path|
  if path =~ /hipster_names\.txt\z/
    @name_file = path
    break
  end
end

name_list = File.open(@name_file, "r").read.split

puts name_list.sample.downcase
